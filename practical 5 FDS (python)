"""Write a Python program to compute following operations on String:
a) To display word with the longest length
b) To determines the frequency of occurrence of particular character in the string
c) To check whether given string is palindrome or not
d) To display index of first appearance of the substring
e) To count the occurrences of each word in a given string"""

a=input("Enter a sentence : ")
print()


#a) To display word with the longest length

b=max(a.split(), key=len)
print("Longest word is : ",b," \nAnd its length is : ",len(b))
print()

#b) To determines the frequency of occurrence of particular character in the string

c={}
for i in a:
    if i in c:
        c[i]+=1
    else:
        c[i]=1

print("Count of all characters in sentence is :\n "+ str(c))
print()


#c) To check whether given string is palindrome or not

print("Original sentence is : " +a)
d=reversed(a)
if list(a)==list(d):
    print(a+" is a palindrome")
else:
    print(a+" is not a palindrome")
print()

#d) To display index of first appearance of the substring

e=str(input("Enter a substring to find : "))
x=a.find(e)
if x==-1:
      print("Substring not found")
else:
    print("Index of first appearance of the substring"+e+" is : ",a.find(e))
    print("Substring is found")
print()

#e) To count the occurrences of each word in a given string

print("Following are the count the frequency of each word in a given string")
y=a.split()
z=[]

for i in y:
    if i not in z:
        z.append(i)

for i in range(0,len(z)):
    print("count of frequency",z[i], "is :", y.count(z[i]))
